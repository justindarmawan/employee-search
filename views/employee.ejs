<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Employee Search</title>
    <link
      href="https://unpkg.com/treeflex/dist/css/treeflex.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <div style="display: flex">
      <h1>
        <a style="color: black; text-decoration: none" href="employees"
          >Employee Hierarchy</a
        >
      </h1>
      <div style="display: flex; margin-left: 50px; align-items: center">
        <form action="/employees" method="post" encType="multipart/form-data">
          <input type="file" name="file" />
          <input type="submit" value="Upload" />
        </form>
      </div>
    </div>
    <% if (locals.fileContents) { %>
    <h2><%= locals.name %></h2>
    <div class="tf-tree"><%- include('list'); %></div>
    <% } %>
  </body>
</html>
